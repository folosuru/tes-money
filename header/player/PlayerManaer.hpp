#include <player/Player.hpp>
namespace tes {
class PlayerManager {
public:
    /*
promise<Player> get() {
if (this->players.contains(name)) {
    return this->players.at(name);
} else {
    if (all_player_index.contains(name);) {
        return // DBから読み出す
    } else {
        return // 新規
    }
}

}
    */
};
}
